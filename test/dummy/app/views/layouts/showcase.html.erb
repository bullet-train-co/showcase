<!DOCTYPE html>
<html>
  <head>
    <title>Showcase</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= stylesheet_link_tag "application" %>

    <script type="module">
      import { Application, Controller } from "https://unpkg.com/@hotwired/stimulus/dist/stimulus.js"
      window.Stimulus = Application.start()

      Stimulus.register("welcome", class extends Controller {
        static targets = [ "greeter" ]
        static values = { yell: Boolean }

        connect() {
          let greeting = this.hasGreeterTarget ? `Welcome, ${this.greeterTarget.textContent}!` : "Welcome!"
          if (this.yellValue) greeting = greeting.toUpperCase()

          console.log(greeting)
          this.dispatch("greeting", { detail: { greeting } })
        }
      })

      Stimulus.register("showcase-event", class extends Controller {
        static targets = [ "relay" ]
        static values = { events: Array }

        connect() {
          this.eventsValue.forEach((name) => this.element.addEventListener(name, (event) => this.emit(event)))
        }

        disconnect() {
          this.eventsValue.forEach(this.element.removeEventListener)
        }

        emit(event) {
          console.log(event)
          this.relay(event)
        }

        relay(event) {
          if (!this.hasRelayTarget) return

          const { controller, ...detail } = event.detail

          const node = document.createElement("div")
          node.innerHTML = JSON.stringify({ type: event.type, detail })
          this.relayTarget.appendChild(node)
        }
      })
    </script>
  </head>

  <body>
    <%= render "showcase/root" %>
  </body>
</html>
