<details open>
  <summary><%= section.name.titleize %></summary>

  <ul>
    <% section.displays.each do |display| %>
      <li><%= link_to display.title.titleize, showcase_display_path(section.name, display.title) %></li>
    <% end %>
  </ul>
</details>
