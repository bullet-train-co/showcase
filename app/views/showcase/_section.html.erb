<details open>
  <summary><%= section.name.titleize %></summary>

  <ul>
    <% section.displays.each do |display| %>
      <li><%= link_to display.name.titleize, showcase_display_path(section.name, display.name) %></li>
    <% end %>
  </ul>
</details>
